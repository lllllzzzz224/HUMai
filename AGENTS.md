# 机械臂项目协作规则

## 目录约定

- `records/`：存放实验、方案和过程记录。
- 文件名使用 `YYYY-MM-DD-主题.md`，主题用简短中文描述。

## 记录规范

- 区分“目标”“已确定内容”“待确认问题”和“后续实验”，不要把设想写成已实现事实。
- 尽量保留原始输入中的关键措辞，同时将内容整理成可执行、可验证的步骤。
- 新实验结果追加到对应记录中，并注明日期、现象和结论。
- 图片、日志或代码证据应使用相对路径链接，避免只写无法复查的结论。

## 清理约定

- 临时文件不进入 `records/`。
- 过时方案不直接删除；标记为“已废弃”，并写明替代方案。

## 子智能体（Subagent）调度规则

遇到以下两类指令时，主智能体必须通过 `invoke_subagent` 委派专门的子智能体独立执行，以隔离海量中间输出并保证主上下文整洁：

1. **抓取与检索类任务**：
   - 凡是用户发出“去抓……”、“抓取数据/资源”、“网页爬取”、“外部检索与资料搜集”等指令，一律调度子智能体执行。
   - 子智能体在隔离上下文中完成请求、过滤与数据抽取，仅向主智能体交付核心结论或结构化结果文件路径。

2. **文档起草与制作类任务**：
   - 凡是用户发出“做一个……文档”、“撰写报告”、“整理输出说明书/方案”等成篇文档的制作指令，一律委派子智能体执行。
   - 子智能体负责草拟正文并依据项目规范落盘（如按规则写入 `records/YYYY-MM-DD-主题.md`），完成后由主智能体复核并向用户汇报交付。
