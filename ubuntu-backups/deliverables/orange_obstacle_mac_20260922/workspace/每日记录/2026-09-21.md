# 2026-09-21 每日进度

- 用户已用枝条遮挡橘子，要求按当前标准处理；本轮采用已讨论的无动作感知/规划预检范围，连接状态未知的枝条按不可穿越障碍处理。
- 12:22 通过 D435 直接取得三组当前 RGB-D，保存到 `reports/branch_observation_20260921_122225/`。相机采集结束后已停止该临时采集流。
- 现有模型的全幅离线检测在三帧中均检出中右侧完整入画橘子，置信度 0.8283–0.8541，目标掩膜内有效深度 91.72%–93.29%；这不等于完整果心定位或避障规划通过。
- 查询 ROS 域 0 和 42 未发现相机/规划场景话题，进程检查未见相机、MoveIt、RViz。自动障碍建模仍只有设计，没有新增实现。本轮未启动规划栈、未写入碰撞场景。
- 已记录预检结果 `NOT_RELEASED_FOR_MOTION`：缺少同步机械臂姿态/TF、枝条碰撞场景和完整通道验证。未发送机械臂或夹爪动作，不代表场景已被证明无路可走。
- 完整预检前须补齐自动障碍建模与同步坐标，再进行仅规划验收。具体证据和结论见本轮报告目录中的 `预检结论.md`。

## 用户要求继续后的坐标采集

- 已整理实施计划 `development/hand_eye_calibration/apple_pick_v2/docs/superpowers/plans/2026-09-21-static-obstacle-preview.md`，通过异步选项请求用户审阅；产品模块尚未开始实现。
- 只读控制器状态查询成功；单独启动 D435 相机后取得 6 组 RGB-D、真实关节和内部相机 TF 的同步观察，保存于 `reports/branch_registered_20260921_145529/`。采集稳定性最大跨度 0.005°，最长前后状态读取区间 0.1069 s；没有机械臂或夹爪动作。
- 已将末份观察导出为 `base_link` 下 86,851 个采样点及 PNG 预览。输出属于观测点云，尚非碰撞模型或可执行规划。
- 最新画面中原中右侧橘子不再可见，已询问是移走还是完全遮挡；不得继续使用中午目标坐标。
- 新确认：实际手眼启动文件与原始标定 YAML 有差异；安装 URDF 的夹爪整体盒宽 80 mm，而当前配置开爪 85 mm，需要核查真实包络。相机碰撞盒需显式加入独立预览场景。
- 本轮只读采集完成后已向自己启动的相机启动进程 PID 866313 发送 SIGINT；相机节点 PID 866355 的日志确认传感器关闭、进程正常结束，启动会话退出码为 0。

## 晚间继续：静态障碍预览落地

- 用户再次“继续”，完成当前开发副本的独立几何、离线导出、场景读回、plan-only与RViz显示模块；实际运行脚本、手眼标定和TCP未修改。
- 复查最近15:10历史橘子帧：全帧0检出，实际56裁块策略最高0.51435，但只覆盖果实一片，不能当完整抓取目标。新采集检查无相机设备，未尝试运动。
- 三组历史数据重放成功；带同步姿态的14:56基座快照3071体素写入隔离MoveIt，加入相机近似盒，完整读回一致。15:10数据仅相机系6924体素，未混用更早姿态。
- 合成25mm障碍使直连13/21状态碰撞，RRTConnect绕行117个插值状态通过；全阻挡碰撞输入被拒绝。只删自有合成对象后原场景摘要恢复。不是橘子真实预抓取验收。
- 独立审查发现同ID盒缩小导致旧占据丢失和FK关节顺序错配，均补失败回归后修复；另补超量程深度不得判自由测试。95项测试全部通过（新增23项，基线72项）。
- 收尾停止本次独立模型/MoveIt/RViz/回放节点；MoveIt的SIGINT关闭超时由launch追加SIGTERM。没有机械臂/夹爪动作，无实际目录部署。
- 成果：reports/obstacle_preview_validation_20260921_214559/验证结论.md，含RViz图、合成路线图、原始读回、测试与识别证据。接下来相机恢复后补同步当前场景与真实候选，不重复把历史成功当现场通过。
- 本功能已提交开发分支 codex/fast-multi-fruit，提交9d3333f；保留先前4个未跟踪文件，没有合并或部署。主机PID检查确认本次预览进程均已结束。
