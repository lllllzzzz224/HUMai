# 同高度重新对齐预检

本轮只准备当前高度横向对位，不下降、不闭爪或抓取；实际运动仍等待监督审批。相机支架是否被移动尚未独立确认，以下基座坐标依赖旧手眼；只允许高位人工比对，不作为近抓取精度认证。

6组新鲜RGBD/真实关节读数：场景与上一轮近位只读图一致，右下完整独立橘子像素(383,345)，观测表面中位[.364122776,-.278875569,-.027042112]m；六帧位置跨度[.062,.127,.254]mm。当前实测TCP[.316023488,-.231270782,.170106089]m。候选仅将XY移到新观测表面XY，Z维持.170106089m，姿态保持不变，无球心推断和历史XY补偿。

保护桌面顶面-.055m，Link6附加工具160×160×220mm假设盒以及相机盒保留。旧115体素保留为保守占用，新增当前RGBD投影96体素；旧障碍保留不是认定旧物位置仍正确。场景读回验证对象级与primitive级组合位姿，尺寸、相机旋转矩阵及数量。

全路径预检通过：18点，FK拉伸后12.7026秒，最大采样段平均速度7.273mm/s；不保证连续瞬时速度。所有样本碰撞检查继承附着体。TCP最低.170104m、工具盒最低.070717m，工具对保护桌净空125.72mm，对当前观测果面竖向净空97.76mm；该竖向值不是实际指尖距离。相机盒最低.254959m。

轨迹SHA256：2b6530857eb37eba44e4eefe28f0093ace012e399baf9bae04a8f94def26c4e9。

execute_alignment_once.py只允许单次显式执行，独立目录锁不影响旧锁；本次无任何夹爪命令。执行前重施加/读回保护、哈希、实时TCP/关节/新鲜度及逐点碰撞门禁；异常硬停止topic确认响应与关节静止后取消action。到位停止，不返回，不重试。

## 实际执行结果

监督验收并启动录像后完成一次同高横移，最终TCP=[.364005817,-.279064569,.170197847]m，反馈到指令位置误差0.24mm，稳定1秒。未发送任何夹爪指令、无下降/返程，终点停止等待人工比对。执行器进程退出0，execution.log及execution_once.lock所指JSON保存。反馈误差不代表物理对位精度。
