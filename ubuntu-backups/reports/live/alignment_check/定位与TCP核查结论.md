# 定位与 TCP 只读核查

本次使用 19:48:44 开爪核验日志、同步录制的 RGB 图像/相机内参/TF，以及用户提供的外部照片。没有追加机械动作或修改定位补偿。

## 已确认

- 实际冻结目标：[0.2689947, -0.2628320, -0.0487851] m。
- 运动前静止图像 frame_0101.jpg 中，投影为 (449.0, 319.4) px，落在俯视图最下方靠电线的橘子果身内。此投影用于确认选取对象，不能独立证明手眼绝对精度。
- VERIFY 静止图像 frame_0281.jpg 中，同一冻结目标投影为 (389.9, 515.9) px；图像高度 480 px，球心在下边界之外。相机光学坐标 Z 约 160.5 mm。
- 代码球面拟合入口只使用深度 0.20～1.00 m 的点。当前目标近距离点云可能被筛掉；另有中心深度加半径的回退方法，因此“关闭 XY 修正”不能等同于“每帧都使用球面拟合”。日志没有逐帧记录拟合方法，不能追认历史每轮采用了哪种方法。
- 下探后仍有诊断输出，但最后一条中心 [0.331442,-0.231174,0.130761] m 与冻结中心显著不同；不能把它直接视为同一颗果实的更新位置。记录没有诊断图像时间戳关联，不能仅据此归因到单个算法步骤。
- 当前单轮抓取使用冻结目标，下探后没有视觉重新对准；上述近距离输出变化不是已确认的抓取失败原因。
- 实时 Link6→tcp_link 平移为 [0,0,0.138] m，与历史定义一致。历史文件将它定义为两指夹持中心，而不是指尖。
- 正式抓取仍额外设置球心上方 15 mm；VERIFY 是球心上方 30 mm。当前外部照片只能用于 VERIFY 的定性检查，不能视作正式抓取高度的实测。

## 尚未确认

- 外部照片没有标定，且为单一斜视角，不能从图中得到 base_link 下可信的毫米补偿。
- TCP=138 mm 是否适用于当前实物指形、果径和闭合状态，需要独立实测。软件 FK/TF 一致并不等于实物夹持中心一致。
- 现有证据不足以在 XY 偏差、Z 包络不足、邻果干涉和夹持力不足之间确定唯一根因。

## 后续有效测量

固定一颗无遮挡橘子，使用能同时看见两指与果实的固定外部视角，记录从开爪到接触前的相对位置。若需要标定绝对偏差，使用已知尺寸标记/测量工具或人工对准 TCP 的独立参考；从程序目标与对准后坐标计算差值，多位置复测。不能从腕部相机同一套 TF 的回投影直接估算手眼修正。

机械臂本次继续保持 VERIFY 开爪停止状态，没有自动返回 SCAN。
