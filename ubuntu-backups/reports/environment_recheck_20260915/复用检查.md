# 搬动后实验环境复用检查（2026-09-15）

结论：旧软件和识别模型可继续使用；当前不具备直接复跑旧抓取轨迹的验证条件。

用户确认整体搬动，相机/夹爪支架与底座固定未变。原 Link6 到 camera_link 手眼变换先保留，但尚未在新环境核验精度。桌面、盆与目标相对基座的位置需要重新确认，旧 measured_table 的 -0.072 m 以及固定盆坐标不是本场地测量值。

相机仍为 USB 3.2 / 5 Gbps。恢复相机驱动时补上旧定位节点必需的 align_depth.enable=true。30Hz 实测彩色有帧、对齐深度0帧，伴随 Frame Corrupted；重启到640×480@15Hz后，10秒彩色144帧、对齐深度136帧，非零深度比例78.1%。15Hz仍有残帧警告，因此仅确认能够出图，未证明长期稳定，也未确定故障根因。相机当前保持15Hz运行，未改生产启动配置。

使用原 yolo11n-seg.pt 和 /home/li/ros_yolo_env/bin/python 对当前照片离线推理成功，识别到3个橘子框和盆框；部分橘子位于画面边缘，结果不是可抓取目标验收。单张CPU推理约459ms，不代表完整识别流程耗时。

ROS/Python/模型依赖此前导入通过。当前未启动完整rm_driver/MoveIt/手眼TF/在线3D定位链，因此未确认在线定位、规划或夹爪通讯可用。机械臂最初关闭，之后有线链路恢复；本次只读查询另存arm_status.json。未发送任何运动或闭爪命令。

证据：camera_status.json、current_color.jpg、current_depth.npy、recognition.json、recognition.jpg、arm_status.json。

恢复抓取前待完成：持续深度稳定性排查；恢复实时关节/TF和在线定位并核查对位；重新测量桌面与盆位置并验证碰撞保护及路径。不得将先前程序PASS视为物理抓取成功。
