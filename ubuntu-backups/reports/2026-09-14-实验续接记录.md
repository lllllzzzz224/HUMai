# 实验续接记录 · 2026-09-14

用户要求保存，暂停新试验。根负责复盘，子智能体 a 唯一负责已批准的机器人执行。当前目标仍为确认单颗橘子进入盆中，尚未确认成功。恢复时先读取本记录末尾最新状态和实际日志，不自动重跑。

## 已确认现象

- 多轮路径执行 PASS 不代表夹到橘子；夹爪只返回位置写入 ACK，没有力/实际包络反馈。
- 用户确认夹指确实闭合，但没有包住橘子；随后明确指出指尖碰桌。摩擦不足暂未证实。
- 两颗橘子同时处于夹指区域的干扰此前存在，后来已改为单橘对照，仍出现失败。
- 先前 XY 关闭/开启、球心上方 15 mm/10 mm 的尝试均未确认成功。不得再用这些低位参数直接抓取当前桌面。

## 碰桌原因与本轮修正

旧程序以拟合球心减半径推算桌面，约 -0.104 m；10 组 RGBD 测量的纸面在上一目标处约 -0.0765 m，相差约 27 mm。拟合残差 1.6 mm 不能当作绝对标定精度。

当前临时 measured_table/config.yaml：测量支撑面 -0.072 m，加 2 mm 保护至 -0.070 m；抓取位为球心上方 40 mm，VERIFY 为上方 50 mm；XY 补偿开启，水平 yaw 偏置 90°，pitch 0；开爪命令 80 mm、闭爪命令 2 mm，闭合等待 2.5 秒。yaw 不是夹指开合夹角，位置命令不是力反馈。

run_measured_table.py 保留相机碰撞体并抬高支撑面保护；只允许预检或显式单次完整入盆执行。生产默认文件未同步此次临时高度保护，不可绕过临时保护直接执行旧默认。

新预检通过；新目标位置变化约 40 mm，已核对 before.jpg 为测量纸面区域内单颗橘子，模型底端距新保护面约 9 mm。实际夹指外形仍未完全建模，不能将此视作实测间隙。

## 保存内容

- reports/live/table_audit/：RGBD 桌面测量、平面拟合和说明。
- reports/live/single_orange_control/measured_table/：本轮配置、临时运行器、预检日志、before.jpg、alignment.avi、731 帧录像元数据 recording.json、关键帧、复盘.md、录像/采图辅助脚本。
- reports/live/single_orange_control/：此前各轮日志、图像与真实失败记录。

## 保存时的执行进度（待最终回报补全）

20:42:46 a 已启动获准的唯一一次 measured_table 完整单轮，重新采样与提高桌面保护后的全路径预检通过，抓取目标 z=-0.0287246 m。最后已收到回报：到达 VERIFY 并完成 GRASP 下探，进入闭爪阶段。用户随后要求保存；已明确通知 a 不得启动下一轮，并要求核实后台进程与最终机器人状态。此段不是当前状态确认，须以末尾追加为准。

## 后续原则

先核实上一轮最终日志与盆中实物，不能凭 PASS 宣称成功。若未成功，区分未包住和夹住后滑落；保留测量桌面保护，不再次盲目降低抓取高度，也不盲目归零或加大夹持命令。用户本次要求保存后不自动发起新的物理试验。

## 最终状态补记

a 已确认原执行进程正常退出（exit 0）：20:43:30 返回 SCAN，夹爪打开命令 80 mm，最终 SCAN 最大关节误差 0.01993 rad；用户要求保存后没有新运动。单轮约 44 秒。完整日志 single_apple_grasp_20260914_204246.json，程序状态 PASS_PICK_PLACE_BASIN_COMPLETE 仅指动作流程。

根随后只读采集 after.jpg：橘子仍在盆外纸面上，盆内可见苹果，本轮未实现橘子入盆。无法从结束图单独判断中途是否短暂夹起或滑落。真实结果另存 physical_outcome.json。当前暂停新试验，机械臂最后确认处于 SCAN，夹爪命令打开。
