# 单次下降 20 mm 尺测诊断（2026-09-16）

用户报告实际夹指最低端至桌面竖直距离为 27 cm，并明确确认测量位置。模型保守工具包络最低角与实际指尖并非同一点，因此没有把约 11.6 cm 的差直接写入标定或下降补偿。

本次只执行保持 XY、姿态和夹爪状态的单次竖直下降 20 mm，供用户以前后尺测验证实际位移。用户在执行前明确回复“已移开，可以执行这一步”，确认尺子和手已离开活动范围。

## 环境恢复与检查

只读核实宿主 ROS 节点和相关进程离线后，恢复真实机械臂驱动、RSP、独立 MoveIt 与相机。规划准备阶段禁止轨迹执行，之后才启动单实例控制端；未启动假关节发布器、GUI 或自动抓取程序，未清除安全停机或发送恢复运动指令。

恢复全部 115+96+81 个历史障碍体素和桌面/工具/相机保护，并根据新鲜 RGBD 加入 55 个当前障碍体素。新图显示水果少量变化；当前纸面局部估计约 Z=-0.0813 至 -0.0800 m，保护桌面仍为 -0.055 m。

冻结轨迹 SHA256：`2622b0ecae3e1b27e39c3c08b1a46a9da2defc36b23c4bff3ad9d7324d4a6185`，5 点，3.739855705 秒。A 对控制器三次样条 98 个样本检查碰撞和限位；根独立以 URDF 和 20 ms 网格复算 191 点，最低工具盒 Z=0.050813431 m，桌面保护净空约 105.81 mm，距当前附近最高可见点的垂直净空约 71.11 mm。上述均为模型采样检查，不是实际指尖尺测值。

根复核单次执行脚本，包含只允许 [0,0,-0.020] m 位移、冻结轨迹与样本哈希、当前起点、新鲜反馈、场景、硬停门禁。执行接口可用且硬停订阅数为 2。

## 执行结果与待测项

执行记录 `../vertical_diagnostic20_20260916/execution_20260916_190300.json`：`PASS_VERTICAL20_DIAGNOSTIC_ONLY`，`gripper_commands=false`、`closed=false`、`return=false`。最终 ROS 反馈 TCP=[0.363555189,-0.281603801,0.150571970] m，日志报告目标误差约 0.49 mm 并稳定 1 秒。

执行后 12 次实际控制器读取跨 1.714 秒，错误码全 0，最大关节跨度 0.006°、TCP 各轴跨度不超过 0.047 mm，确认短时静止。A 计算实际关节 FK 前后均值 Z 变化约 -19.866 mm；这仍是模型反馈位移，需要尺测核对实际指尖。

根查看末帧，录像已停止，保存 372 帧。机械臂保持不动，无后续自动下降或闭爪。已请用户用同一把尺、同一指尖和桌面基准再次测量；相对位移一致时预期约 25 cm，尚未收到实际后测值，不能宣称尺测验证完成。
