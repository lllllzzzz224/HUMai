# 同一历史画面的平铺诊断

输入仍为2026-09-21 15:10:16旧帧，不是当前现场。未连接硬件，未改模型或置信度阈值。

实际定位器当前同时使用240×240/步长120细网格，以及宽480/步长160全高条块。本次从源码提取纯函数复用，在CPU离线运行56个裁块，类别47/49，阈值0.15。未运行ROS、目标跟踪、球拟合、基座变换和抓取验收。

细网格最高非边缘橘子框置信度0.51435；480全高条块最高为0.35247。按既有图像层规则选出的最高置信度掩膜仅覆盖可见橘子右侧小片区域，约31×53像素的框、1037个掩膜像素，不是完整果体。框不接触裁块边缘不等于果体完整，不能排除遮挡叶片混入；该掩膜不能直接作为已验收目标几何。

全帧0检出的diagnostic.json、target_mask.npy、annotated.jpg保留原状，并在平铺诊断中保存其SHA-256用于核验。结果支持裁块策略影响这一帧的检出，但尚未证明可靠定位或可抓取。
