# 新环境下真机橘子抓取入盆验证

## 目标

1. 验证更换实验环境（不同桌面、不同光照）后，RM65 机械臂与 RealSense D435 视觉抓取系统的环境自适应能力。
2. 验证无需手动测量桌面距离与无需重新建模目标水果的情况下，系统能否完成端到端视觉定位、自适应支撑面推导与单轮真机采摘入盆全闭环。

## 已确定内容

- **通信与节点环境**：
  - Ubuntu 主机（`10.77.0.2`）与机械臂底座（`192.168.1.18`）网络通信正常。
  - 抓取全栈服务统一部署在 Ubuntu 后台 tmux 会话 `rm_stack`（`ROS_DOMAIN_ID=42`）。
- **空间几何与自适应机制**：
  - 桌面支撑面高度采用动态视觉推导：$Z_{\text{table}} = Z_{\text{fruit}} - R_{\text{fruit}} + \Delta_{\text{inflation}}$，动态生成局部碰撞保护面（`support_guard`，顶面 $Z = -0.101\text{ m}$）。
  - 水果采用 YOLOv11 实例分割 + 点云 3D RANSAC 球拟合动态解算，无需静态 3D 模型。
  - 盆子落料点位姿维持既有标定参数：`basin_place = [0.464, 0.006, -0.020] m`。

## 过程与现象

1. **环境准备与状态重置**：
   - 终止旧的孤立诊断测试进程，通过 `./arm_ctl start --target orange` 一键启动驱动、控制器、MoveIt 2 与视觉定位节点。
   - 针对机械臂启动时的微小关节角偏差（Joint 5 偏差约 7.6°），调用平缓回位脚本回到标准固定鸟瞰位（SCAN），最大关节误差控制在 $0.013\text{ rad}$，校验通过。
2. **目标识别与多段轨迹预检**：
   - 视觉节点稳定捕获工作区内橘子：
     - 原始三维中心：$[0.4130, -0.1790, -0.0639]\text{ m}$
     - 拟合半径：$39.3\text{ mm}$
   - MoveIt 完成链式规划预检，全部路径（`SCAN -> PREGRASP -> VERIFY -> GRASP -> LIFT -> BASIN_APPROACH -> BASIN_PLACE -> BASIN_APPROACH -> SCAN`）求解成功，求解率均为 100%（`fraction=1.0000`）。
3. **真机全流程执行**：
   - 下发真机抓取指令（`single` 模式）：
     - `21:16:56` 夹爪配置并开爪至 $80.0\text{ mm}$；
     - `21:17:10` 到位 `VERIFY`，稳定到位误差 $0.64\text{ mm}$；
     - `21:17:12` 笛卡尔下潜到位 `GRASP`，稳定到位误差 $1.24\text{ mm}$，执行保守闭爪（目标位 $2.0\text{ mm}$）；
     - `21:17:18` 垂直起吊到位 `LIFT`（上升 $70\text{ mm}$），稳定到位误差 $0.40\text{ mm}$；
     - `21:17:22` 横向平稳转运至盆口上方 `BASIN_APPROACH`；
     - `21:17:27` 笛卡尔下潜到位 `BASIN_PLACE`，稳定到位误差 $0.55\text{ mm}$，夹爪平稳松开释放果实入盆；
     - `21:17:32` 垂直回抽至盆口上方，随后平稳返回固定鸟瞰位 `SCAN`；
     - `21:17:37` 夹爪重新复位至 $80.0\text{ mm}$ 开口，单轮抓取圆满完成。

## 结论与数据证据

- **最终状态**：`PASS_PICK_PLACE_BASIN_COMPLETE`，总耗时约 44 秒。
- **定位与到位精度**：
  - 各关键工位实测稳态偏差均控制在 $1.3\text{ mm}$ 以内（`LIFT` 误差 $0.40\text{ mm}$，`BASIN_PLACE` 误差 $0.55\text{ mm}$）。
  - 动态支撑面推导精准，全流程未触发任何物理碰撞报警或急停。
- **证据文件**：
  - 完整真实运行日志：[2026-09-16_single_orange_basin_success.json](assets/2026-09-16_single_orange_basin_success.json)

## 待确认问题与后续实验

1. **落料盆物理对齐**：当前测试证实原有盆位参数在新环境下落料顺畅。若后续移动盆子，需执行 `./arm_ctl align-basin` 重新确认。
2. **多果连续采摘**：本次单轮验证成功后，下一步可在视野内摆放多个橘子，测试连续采摘模式（`./arm_ctl grasp --mode continuous`）。
