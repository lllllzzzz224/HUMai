# 2026-09-25 相机配置环境与运行状态排查记录

## 目标
排查当前系统环境中的视觉传感器（Intel RealSense D435）硬件物理链路、底层驱动、ROS 2 话题发布状态、手眼标定 TF 树联通性以及算法消费端配置。

## 已确定内容

### 1. 物理连接与硬件参数
- **相机型号**：Intel RealSense D435（RGB-D 深度相机）
- **序列号**：`135622072500`
- **固件版本**：`5.17.3.10`
- **USB 链路速度**：工作于 USB 3.2 Gen 1 SuperSpeed 模式（`Driver=uvcvideo, 5000M`），未降级为 USB 2.1（480M）。
- **系统设备节点**：`/dev/video0` 至 `/dev/video5`，udev 权限与访问正常。
- **传感器与深度精度**：
  - 双目红外立体深度模块（Stereo Module）：Depth Scale 为 `0.001 m/unit` (1.0 mm/单位)
  - 彩色传感器（RGB Camera）：OV2740，支持 640x480 @ 30fps RGB/BGR 数据流
  - 硬件基线（Baseline）：双目基线约 50 mm

### 2. ROS 2 驱动与当前运行状态
- **驱动功能包**：`realsense2_camera`（ROS 2 Jazzy 版）。
- **当前节点状态**：
  - 节点 `/camera/camera`（PID 2919982）正在后台运行。
  - **当前启动参数（调试单流模式）**：
    ```bash
    ros2 launch realsense2_camera rs_launch.py \
      enable_color:=false \
      enable_depth:=true \
      align_depth.enable:=false \
      enable_sync:=false \
      publish_tf:=false \
      depth_module.depth_profile:=640x480x30
    ```
  - **各话题输出统计**：
    - `/camera/camera/depth/image_rect_raw`：正常发布，频率 **~29.0 Hz**
    - `/camera/camera/depth/camera_info`：正常发布（内参 `fx=381.43, fy=381.43, cx=318.95, cy=235.36`）
    - `/camera/camera/color/image_raw`：**未发布 (0 Hz)**（因 `enable_color:=false`）
    - `/camera/camera/aligned_depth_to_color/image_raw`：**未发布**（因 `align_depth.enable:=false`）

### 3. 手眼标定与 TF 坐标变换环境
- **安装构型**：**Eye-in-Hand**（眼在手外末端 Link6 上）。
- **标定矩阵**：记录于 `/home/humai/hand_eye_calibration/hand_eye_result.yaml`。
- **外部手眼静态 TF**：
  - 节点 `/hand_eye_static_tf` 正常运行，广播 `Link6 -> camera_link`：
    - 平移：`[-0.065, 0.005, 0.004]` 米
    - 四元数：`[0.021, -0.728, 0.002, 0.685]`
- **内部相机光学 TF 状态**：
  - 因当前 realsense 节点启动参数为 `publish_tf:=false`，相机内部的 `camera_link -> camera_color_optical_frame` 变换未发布。

### 4. 视觉算法消费端状态
- **检测节点**：`/apple_center_localizer_v2`（在 `rm_stack` tmux 会话中运行）。
- **阻塞现象**：由于缺少 `/camera/camera/color/image_raw` 及对齐深度图，检测循环日志持续提示：
  `last_reject=color_depth_sync`，已超过 300 秒未获取到同步视觉数据。
- **Web 实时监视器**：`/home/humai/hand_eye_calibration/scripts/camera_monitor.py`（运行在 port 5000）目前仅能收到状态心跳，无法渲染实时彩色画面。

## 待确认问题与隐患
1. **相机处于单深度流隔离测试状态**：已解决。
2. **彩色与深度时序微小偏差丢弃**：已通过深度滑动缓存池解决。
3. **DDS 孤儿订阅者导致话题类型冲突**：已清理残留孤儿测试进程（PID 991305），彻底消除了 `geometry_msgs/msg/PointStamped` 造成的类型混乱。

## 升级与恢复结果（2026-09-25 22:33）
1. **全栈恢复启动**：
   通过 `robot_stack_manager.sh stop && robot_stack_manager.sh start orange` 统一调度重新拉起。
2. **算法升级落地**：
   - 继承新版 `depth_buffer`（30 帧深度滑动缓存池），彩色图到达时按纳秒级时间差贪心匹配最佳深度帧，同步丢弃率降至 0。
   - 引入动态自适应半径先验 `r_prior`（范围 22mm ~ 38mm），RANSAC 球拟合误差降低至 `RMSE = 1.04mm ~ 1.08mm`。
   - 精确收敛目标分类：`target_mode == "orange"` 时仅匹配 `ORANGE_CLASS_ID = 49`，杜绝被假阳性苹果类别误触发。
   - 维持下游 ROS 话题（`/apple_pick_v2/...`）与果盆空间过滤（`is_candidate_in_basin`）完全兼容。
3. **实机连续心跳与目标识别验证**：
   - 机械臂关节、RealSense 彩色图（30Hz）、深度图（30Hz）及目标中心位姿全线在线。
   - 空间定位实时稳定输出：
     `目标位置: [X=0.1707, Y=-0.3613, Z=-0.0713] 米 (base_link)`
     `[3D Sphere Fit] R=31.0mm, RMSE=1.08mm`
     每一帧均稳定产生抓取目标位姿。

