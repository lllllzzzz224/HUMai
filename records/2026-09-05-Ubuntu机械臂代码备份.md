# Ubuntu 机械臂代码备份

## 目标

把 Ubuntu 主机上实际用于机械臂、手眼标定、苹果抓取和 ROS 2 抓取实验的代码复制到 Mac 本地保存，同时不改变或删除 Ubuntu 原文件。

## 已确定内容

- 来源主机：`li@10.77.0.2`。
- 连接链路：Mac 与 Ubuntu 千兆网线直连。
- Ubuntu 主要真实抓取目录：`/home/li/hand_eye_calibration/`。
- Ubuntu ROS 2 抓取包：`/home/li/ros2_ws/src/rm_rl_grasp/`。
- Ubuntu 主目录独立 Gate 3 探针：`/home/li/gate3_*.py`。
- 本地快照入口：[备份清单](../ubuntu-backups/snapshots/2026-09-05/MANIFEST.md)。

## 复制结果

- `hand_eye_calibration`：507 个文件，约 283 MB。
- `rm_rl_grasp`：2405 个文件，约 170 MB。
- `home-probes`：20 个文件，约 92 KB。
- 抽查真实连续抓取入口、YOLO 模型、ROS launch 文件和 Gate 3 探针，SHA-256 均与 Ubuntu 原文件一致。

## 排除内容

- `.git/` 和 `.worktrees/`。
- `build/`、`install/` 和 `log/`。
- `__pycache__/`、`.pytest_cache/` 和 `*.pyc`。
- 密码、SSH 私钥、Shell 历史和用户配置目录。

## 待确认问题

- 当前快照是备份，不代表代码可在 macOS 上直接运行。
- 后续若要继续开发，应明确以 `hand_eye_calibration` 的真实硬件流程还是 `rm_rl_grasp` 的 ROS 2 实验包作为主线。

## 后续实验

- 在 Ubuntu 上按安全流程验证真实机械臂入口，不从 Mac 直接运行 Linux 驱动库。
- 如 Ubuntu 源码继续变化，创建新的日期快照，不覆盖本次备份。
