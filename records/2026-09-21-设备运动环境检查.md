# 2026-09-21 设备运动环境检查与自检记录

## 目标
全面排查当前主机的机械臂运动控制系统、传感器、ROS 2 运行栈以及算法环境，确认软硬件通信与运动执行就绪状态。

## 已确定内容

### 1. 机械臂本体与运动通信 (RealMan RM65-BI)
- **型号**：睿尔曼 RM65-BI（6 自由度超轻量协作机械臂）
- **固件与算法版本**：Controller `V1.7.1` (2025/06/16), Algorithm `1.5.5`, 动力学模型版本 `2`
- **网络连接**：通过专有 USB 网卡 `enx00e04c3a4178` (`192.168.1.100/24`) 与机械臂 (`192.168.1.18`) 直连。
- **底层连通性**：网络双向连通，ICMP Ping 延迟 ~0.5ms，无丢包；ARP 正常解析 MAC 地址 (`00:00:29:86:91:9d`)。
- **SDK 直连验证**：通过 RealMan 原生 Python SDK (`Robotic_Arm.rm_robot_interface`) 建立 Handle (ID: 1) 成功。
  - 当前机械臂状态：正常 (`err: 0`，无急停或异常报错)
  - 当前关节角度（度）：`[143.171°, 20.262°, 37.389°, 3.779°, 117.512°, -3.312°]`
  - 当前末端位姿（米/弧度）：`X=0.229m, Y=-0.160m, Z=0.433m, roll=3.090, pitch=0.088, yaw=2.587`

### 2. 末端执行器与外设
- **末端夹爪**：通过机械臂末端接口板 RS485 端口 1，采用 Modbus-RTU 协议进行驱动控制（由 `rm_driver` 转发 `/rm_driver/write_modbus_rtu_registers_cmd`）。
- **串口设备**：系统挂载 `/dev/ttyUSB0` (QinHeng CH340 USB 串口芯片)。

### 3. 视觉感知环境 (RGB-D)
- **相机型号**：Intel RealSense D435 深度相机
- **连接方式**：USB 3.0 直连，已挂载 `/dev/video0` 至 `/dev/video5`
- **设备信息**：序列号 `135622072500`，固件版本 `5.17.3.10`
- **驱动库**：在 `ros_yolo_env` 环境中成功通过 `pyrealsense2` (v2.58.4) 查询并建立流通信。
- **标定参数**：已存在手眼标定参数文件 `/home/humai/hand_eye_calibration/hand_eye_result.yaml`。

### 4. 操作系统与计算平台
- **操作系统**：Ubuntu 24.04.5 LTS (x86_64), Linux 7.0.0-31-generic
- **CPU**：12th Gen Intel Core i5-12400 (6 核 12 线程)
- **GPU**：Intel UHD Graphics 730（核显，无独立 NVIDIA 显卡，YOLO / PyTorch 当前使用 CPU 模式运行）
- **内存**：16 GiB (当前空闲 ~11 GiB)

### 5. ROS 2 与 MoveIt 2 运动规划栈
- **ROS 2 发行版**：ROS 2 Jazzy Jalisco (`/opt/ros/jazzy`)
- **ROS_DOMAIN_ID**：`42` (已于 `~/.bashrc` 导出)
- **编译工作空间**：`/home/humai/ros2_ws`
  - 核心功能包：`rm_65_config`, `rm_driver`, `rm_control`, `rm_moveit2`, `rm_ros_interfaces`, `rm_rl_grasp` 等已构建并安装于 `install/`。
- **运动规划器**：MoveIt 2 完整栈安装就绪 (`moveit_ros_move_group`, OMPL 规划器, 运动学求解插件已齐备)。

### 6. 算法与控制 Python 环境
- **环境路径**：`/home/humai/ros_yolo_env` (以及软链接 `/home/humai/anaconda3/envs/yolo11`)
- **关键库依赖**：
  - `robotic-arm`: 1.1.6
  - `rclpy`: 7.1.12
  - `pyrealsense2`: 2.58.4
  - `ultralytics`: 8.4.155 (YOLO)
  - `torch`: 2.14.0+cpu / `torchvision`: 0.29.0+cpu
  - `opencv-python`: 5.0.0.93

## 待确认问题与隐患

1. **`robot_stack_manager.sh` 用户路径硬编码**：
   - 脚本 `/home/humai/hand_eye_calibration/scripts/robot_stack_manager.sh` 内部前几行写死了 `/home/li/...`，而当前系统用户名是 `humai`，直接运行该脚本的命令可能会因找不到路径而报错，建议做一次路径替换同步。

## 后续建议
1. 修正 `robot_stack_manager.sh` 中的路径配置为 `/home/humai/`。
2. 如需启动机械臂全套服务（驱动、MoveIt 节点、相机、手眼 TF），可通过更新后的脚本或 ros2 launch 逐步拉起。
